anthropic>=0.116.0
alpaca-py>=0.30.0
pydantic>=2.0
python-dotenv>=1.0
requests>=2.31

# ML pipeline (build_dataset.py, audit_dataset.py)
pandas>=2.0
numpy>=1.24
pyarrow>=14.0        # parquet; falls back to CSV if absent
# scikit-learn>=1.3  # uncomment when training starts
# lightgbm>=4.0      # gradient boosting for the model itself
